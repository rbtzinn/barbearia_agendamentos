// pages/ShopPublic/styles.ts
import styled from 'styled-components';

export const LocationText = styled.div`
  color: ${({ theme }) => theme.colors.muted};
  margin-bottom: ${({ theme }) => theme.spacing.md};
`;
